version: '3.8'

services:
  mysql:
    image: mysql:8
    container_name: mysql-checkpoint
    restart: always
    environment:
      MYSQL_ROOT_PASSWORD: root
      MYSQL_DATABASE: checkpoint
    ports:
      - "3306:3306"